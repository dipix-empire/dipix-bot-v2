import { MessageEmbed } from "discord.js";
export const footer = {text: `DiPix Bot © Philainel, 2022`}
export const ErrorEmbed = (comment = "Произошла непредвиденная ошибка.") => new MessageEmbed()
	.setColor('#c62828')
	.setTitle('Ошибка!')
	.setDescription(comment)
	.setTimestamp(Date.now())
	.setFooter(footer)
export const SuccesfulEmbed = (comment = "Команда успешно выполнено.") => new MessageEmbed()
	.setColor('#3ba55d')
	.setTitle('Успешно!')
	.setDescription(comment)
	.setTimestamp(Date.now())
	.setFooter(footer)
export const ProcessingEmbed = (comment = "Действие выполняется...") => new MessageEmbed()
	.setColor("YELLOW")
	.setTitle("Выполняется...")
	.setDescription(comment)
	.setTimestamp(Date.now())
	.setFooter(footer)
export const WarnEmbed = (comment: string) => new MessageEmbed()
	.setTitle("Внимание!")
	.setDescription(comment)
	.setColor("YELLOW")
	.setTimestamp(Date.now())
	.setFooter(footer)
export const InfoEmbed = (title: string, comment = "") => new MessageEmbed()
	.setColor("DARK_GREY")
	.setTitle(title)
	.setDescription(comment)
	.setTimestamp(Date.now())
	.setFooter(footer)